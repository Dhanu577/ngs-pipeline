version: '3.8'

services:
  ngs-pipeline:
    build: .
    volumes:
      - ./data:/app/data
      - ./results:/app/results
      - ./config:/app/config
    environment:
      - PYTHONUNBUFFERED=1
    command: run --config /app/config/sample_config.yaml --output /app/results/docker_run
